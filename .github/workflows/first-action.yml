name: First Workflow
on:  workflow_dispatch #wait for the user to manually trigger it

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: |
          echo "command 1!"
          echo "command 2!"
      - name: Print Goodby
        run: echo "Done-Bye!"
      
      

  
